int gcd ( int u, int v ) {
  if (v == 0) return u;
  else return gcd(v, u - u/v*v);
}
void main(void) {
  int x; int y;
  /* Valores fixos para testar a lógica */
  x = 20;
  y = 10;
  output(gcd(x,y));
}
